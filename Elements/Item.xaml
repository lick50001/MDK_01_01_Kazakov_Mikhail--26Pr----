<UserControl x:Class="WpfApp3.Elements.Item"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Loaded="UserControl_Loaded"
             mc:Ignorable="d" 
             Background="White" d:DesignWidth="800" Height="140">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="140"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <Label Content="{Binding Price, StringFormat={}{0} ₱}" FontSize="24" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" HorizontalContentAlignment="Center"/>
            <Button Content="Выбрать" Background="#FF0078D7" Foreground="White" Height="30" Margin="0,10,20,0" BorderThickness="0"/>
        </StackPanel>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Label Content="{Binding Time_start, StringFormat=HH:mm}" FontSize="20" FontWeight="SemiBold"/>
            <Label Content="{Binding From}" FontSize="14" Foreground="Gray"/>
            <Label Content="29 сен, ср" FontSize="11" Opacity="0.6"/>
        </StackPanel>

        <Grid Grid.Column="2" VerticalAlignment="Center" Margin="10,0">
            <Label x:Name="lDuration" Content="" FontSize="11" 
       HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="Gray"/>
            <Rectangle Height="1" Fill="#CCC" VerticalAlignment="Center" Margin="5,0"/>
            <Ellipse Width="4" Height="4" Fill="#CCC" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Ellipse Width="4" Height="4" Fill="#CCC" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right">
            <Label Content="pobeda" FontSize="12" Foreground="Gray" HorizontalAlignment="Right"/>

            <Label Content="{Binding Time_way, StringFormat=HH:mm}" 
           FontSize="22" FontWeight="SemiBold" 
           HorizontalAlignment="Right" x:Name="TimeEnd"/>

            <Label Content="{Binding To}" FontSize="14" HorizontalAlignment="Right"/>
            <Label Content="29 сентября 2021" FontSize="12" Opacity="0.7" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</UserControl>